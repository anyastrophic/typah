from setuptools import setup

setup(
    name='typah',
    version='1.0.0',    
    description='An insanely simple application to auto type. Supports multi line, repeat, random key delays, set key delays. Uses OOP',
    url='https://github.com/logixhot/typah',
    author='Logix',
    license='MIT',
    packages=[],
    install_requires=['keyboard'],

    classifiers=[],
)
